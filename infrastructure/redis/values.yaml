---
architecture: standalone
auth:
  enabled: false
  sentinel: false
networkPolicy:
  enabled: true
  allowExternal: false
  extraIngress:
    from:
      - namespaceSelector:
          matchLabels:
            name: api
        podSelector:
          matchLabels:
            app: api
      - ports:
         - port: 6379