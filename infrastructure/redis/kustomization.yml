apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: redis
resources:
  - namespace.yml
  - release.yml
configMapGenerator:
  - name: redis-config-values
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yml